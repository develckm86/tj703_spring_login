spring:
  application:
    name: l09_spring_login
  datasource:
    url: jdbc:mysql://localhost:3306/login_study
    username: login_study_dev
    password: 1234
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 7777

logging:
  level:
    org:
      springframework:
        security: DEBUG
#security log